{
  "name": "@skycoin4444/detection-event-engine",
  "version": "0.2.0",
  "description": "Bounded deterministic registry for externally produced object-detection events",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check": "npm run build && npm test && npm audit --omit=dev --audit-level=high"
  },
  "keywords": ["skycoin4444", "typescript", "computer-vision", "event-validation"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
